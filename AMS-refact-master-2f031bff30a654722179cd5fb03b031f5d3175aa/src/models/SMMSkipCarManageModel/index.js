/**
fileName    : index.js
writer      : **Input your name here**
reviewers   : **Input reviewers here**
*/

import Immutable from 'immutable';

export default Immutable.fromJS({
  // ...your model
  modalVisible: false,
  modalType: false, //false:批量增加，true:单个增加
});
