import {
  // ELEMENT,
  // NODE,
  // OBJECT,
  ICON,
  NUMBER,
  STRING,
} from '../propertyTypes';
/**
 * define special props from Material UI component
 *
 * @see http://www.material-ui.com/#/components/???
 * @type {Object}
 */
export default {
  backgroundColor: STRING,
  // children: NODE,
  // className: STRING,
  color: STRING,
  icon: ICON, // change attribute, custom
  size: NUMBER,
  src: STRING, // change attribute, custom
  // style: OBJECT,
};
