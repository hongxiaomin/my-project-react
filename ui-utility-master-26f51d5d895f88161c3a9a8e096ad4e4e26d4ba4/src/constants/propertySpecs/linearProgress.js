import {
  STRING,
  NUMBER,
  ENUM,
  // OBJECT,
} from '../propertyTypes';
/**
 * define special props from Material UI component
 *
 * @see http://www.material-ui.com/#/components/???
 * @type {Object}
 */
export default {
  color: STRING,
  max: NUMBER,
  min: NUMBER,
  mode: ENUM,
  // style: OBJECT,
  value: NUMBER,
};
