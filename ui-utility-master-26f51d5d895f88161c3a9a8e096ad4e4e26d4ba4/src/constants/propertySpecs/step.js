import {
  // NODE,
  BOOLEAN,
  // OBJECT,
} from '../propertyTypes';
/**
 * define special props from Material UI component
 *
 * @see http://www.material-ui.com/#/components/???
 * @type {Object}
 */
export default {
  active: BOOLEAN,
  // children: NODE,
  completed: BOOLEAN,
  disabled: BOOLEAN,
  // style: OBJECT,
};
